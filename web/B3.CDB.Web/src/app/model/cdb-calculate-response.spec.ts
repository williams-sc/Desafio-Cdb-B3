import { CdbCalculateResponse } from './cdb-calculate-response';

describe('CdbCalculateResponse', () => {
  it('should create an instance', () => {
    expect(new CdbCalculateResponse(10, 10)).toBeTruthy();
  });
});
